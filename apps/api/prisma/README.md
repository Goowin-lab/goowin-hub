# apps/api/prisma

Reservado para Prisma ORM.

Aqui se ubicaran `schema.prisma`, migraciones y archivos relacionados con el acceso tipado a PostgreSQL. No se crea un schema inicial porque todavia no hay entidades ni reglas de negocio aprobadas.
