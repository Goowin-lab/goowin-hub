# infrastructure/docker

Reservado para configuraciones Docker.

Aqui podran vivir archivos como `docker-compose.yml`, configuraciones de PostgreSQL local, servicios auxiliares y perfiles de desarrollo.

No se agrega un compose ejecutable en esta fase para mantener el repositorio en estructura pura.
